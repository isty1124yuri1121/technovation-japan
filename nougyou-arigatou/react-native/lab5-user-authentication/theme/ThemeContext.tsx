/**
 * A simple React context that can be used to share theme and style information
 * across components.
 */
import React from 'react';

const ThemeContext = React.createContext({});

export default ThemeContext;
